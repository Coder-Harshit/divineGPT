name: Microservices Compose Test

on:
  push:
    branches: [ "main", "deployment" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test-services:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and Run Services with Docker Compose
        run: |
          docker compose -f docker-compose.yml up -d
          sleep 20  # Give services time to boot

#      - name: Test LLM Service
#        run: curl http://localhost:8000/status | grep -o "running"
#
#      - name: Test RAG Service
#        run: curl http://localhost:8001/status | grep -o "running"
#
#      - name: Test T2S Service
#        run: curl http://localhost:8003/status | grep -o "running"

      # # Optional: shut down after testing
      # - name: Shutdown
      #   run: docker compose down
